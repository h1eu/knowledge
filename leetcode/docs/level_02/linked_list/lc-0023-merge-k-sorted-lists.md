---
title: "23. Merge k Sorted Lists"
slug: "lc-0023-merge-k-sorted-lists"
summary: "Linked List • Level 2 • Hard — Merge k Sorted Lists (LeetCode 23)"
tags: ["leetcode", "linked-list", "hard", "level-2"]
topic_tags: ["linked-list", "divide-and-conquer", "heap-priority-queue", "merge-sort", "tournament-sort"]
difficulty: "Hard"
pattern: "Linked List"
level: 2
leetcode_id: 23
url: "https://leetcode.com/problems/merge-k-sorted-lists/"
---

# 23. Merge k Sorted Lists

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-hard">Hard</span> <span class="lc-pill lc-pill-pattern">Linked List</span> <span class="lc-pill lc-pill-level">Level 2</span> <a href="https://leetcode.com/problems/merge-k-sorted-lists/" target="_blank">LeetCode 23 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng <code>k</code> danh sách liên kết đã sắp xếp <code>lists</code>, hãy gộp tất cả thành một danh sách đã sắp xếp và trả về đầu.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> lists = [[1,4,5],[1,3,4],[2,6]]
<strong>Đầu ra:</strong> [1,1,2,3,4,4,5,6]
<strong>Giải thích:</strong> The linked-lists are:
[
  1-&gt;4-&gt;5,
  1-&gt;3-&gt;4,
  2-&gt;6
]
merging them into one sorted linked list:
1-&gt;1-&gt;2-&gt;3-&gt;4-&gt;4-&gt;5-&gt;6
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> lists = []
<strong>Đầu ra:</strong> []
</pre>

<p><strong class="example">Ví dụ 3:</strong></p>

<pre>
<strong>Đầu vào:</strong> lists = [[]]
<strong>Đầu ra:</strong> []
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>k == lists.length</code></li>
	<li><code>0 &lt;= k &lt;= 10<sup>4</sup></code></li>
	<li><code>0 &lt;= lists[i].length &lt;= 500</code></li>
	<li><code>-10<sup>4</sup> &lt;= lists[i][j] &lt;= 10<sup>4</sup></code></li>
	<li><code>lists[i]</code> is sorted in <strong>ascending order</strong>.</li>
	<li>The sum of <code>lists[i].length</code> will not exceed <code>10<sup>4</sup></code>.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Linked List**.

## 💻 Code 5 ngôn ngữ

```cpp
// Merge k Sorted Lists - C++
class Solution {}
```

```java
// Merge k Sorted Lists - Java
class Solution {}
```

```kotlin
// Merge k Sorted Lists - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Merge k Sorted Lists - Swift
func solve() -> Int { return 0 }
```

```dart
// Merge k Sorted Lists - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
