---
title: "2. Add Two Numbers"
slug: "lc-0002-add-two-numbers"
summary: "Linked List • Level 2 • Medium — Add Two Numbers (LeetCode 2)"
tags: ["leetcode", "linked-list", "medium", "level-2"]
topic_tags: ["linked-list", "math", "recursion"]
difficulty: "Medium"
pattern: "Linked List"
level: 2
leetcode_id: 2
url: "https://leetcode.com/problems/add-two-numbers/"
---

# 2. Add Two Numbers

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Linked List</span> <span class="lc-pill lc-pill-level">Level 2</span> <a href="https://leetcode.com/problems/add-two-numbers/" target="_blank">LeetCode 2 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho hai danh sách liên kết không rỗng <code>l1</code> và <code>l2</code> biểu diễn hai số nguyên không âm, các chữ số lưu theo thứ tự ngược (ví dụ <code>2→4→3</code> là 342). Hãy cộng hai số và trả về tổng dưới dạng danh sách liên kết.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>
<img alt="" src="https://assets.leetcode.com/uploads/2020/10/02/addtwonumber1.jpg" style="width: 483px; height: 342px;" />
<pre>
<strong>Đầu vào:</strong> l1 = [2,4,3], l2 = [5,6,4]
<strong>Đầu ra:</strong> [7,0,8]
<strong>Giải thích:</strong> 342 + 465 = 807.
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> l1 = [0], l2 = [0]
<strong>Đầu ra:</strong> [0]
</pre>

<p><strong class="example">Ví dụ 3:</strong></p>

<pre>
<strong>Đầu vào:</strong> l1 = [9,9,9,9,9,9,9], l2 = [9,9,9,9]
<strong>Đầu ra:</strong> [8,9,9,9,0,0,0,1]
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li>The number of nodes in each linked list is in the range <code>[1, 100]</code>.</li>
	<li><code>0 &lt;= Node.val &lt;= 9</code></li>
	<li>It is guaranteed that the list represents a number that does not have leading zeros.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Linked List**.

## 💻 Code 5 ngôn ngữ

```cpp
// Add Two Numbers - C++
class Solution {}
```

```java
// Add Two Numbers - Java
class Solution {}
```

```kotlin
// Add Two Numbers - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Add Two Numbers - Swift
func solve() -> Int { return 0 }
```

```dart
// Add Two Numbers - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
