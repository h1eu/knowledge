---
title: "75. Sort Colors"
slug: "lc-0075-sort-colors-1"
summary: "Matrix & Sorting • Level 2 • Medium — Sort Colors (LeetCode 75)"
tags: ["leetcode", "matrix-sorting", "medium", "level-2"]
topic_tags: ["array", "two-pointers", "sorting", "quicksort", "bubble-sort"]
difficulty: "Medium"
pattern: "Matrix & Sorting"
level: 2
leetcode_id: 75
url: "https://leetcode.com/problems/sort-colors/"
---

# 75. Sort Colors

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Matrix & Sorting</span> <span class="lc-pill lc-pill-level">Level 2</span> <a href="https://leetcode.com/problems/sort-colors/" target="_blank">LeetCode 75 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng <code>nums</code> với các đối tượng <code>0</code> (đỏ), <code>1</code> (trắng), <code>2</code> (xanh), hãy sắp xếp in-place theo thứ tự <code>0,1,2</code>.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<div class="example-block">
<p><strong>Đầu vào:</strong> <span class="example-io">nums = [2,0,2,1,1,0]</span></p>

<p><strong>Đầu ra:</strong> <span class="example-io">[0,0,1,1,2,2]</span></p>

<p><strong>Giải thích:</strong></p>

<p>The array has two 0s, two 1s, and two 2s. Sorting them in-place places all 0s first, then all 1s, then all 2s.</p>
</div>

<p><strong class="example">Example 2:</strong></p>

<div class="example-block">
<p><strong>Input:</strong> <span class="example-io">nums = [2,0,1]</span></p>

<p><strong>Output:</strong> <span class="example-io">[0,1,2]</span></p>

<p><strong>Explanation:</strong></p>

<p>The array has one each of 0, 1, and 2, arranged in-place in the order 0, 1, 2.</p>
</div>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>n == nums.length</code></li>
	<li><code>1 &lt;= n &lt;= 300</code></li>
	<li><code>nums[i]</code> is either 0, 1, or 2.</li>
</ul>

<p>&nbsp;</p>
<p><strong>Follow up:</strong>&nbsp;Could you come up with a one-pass algorithm using only&nbsp;constant extra space?</p>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Matrix & Sorting**.

## 💻 Code 5 ngôn ngữ

```cpp
// Sort Colors - C++
class Solution {}
```

```java
// Sort Colors - Java
class Solution {}
```

```kotlin
// Sort Colors - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Sort Colors - Swift
func solve() -> Int { return 0 }
```

```dart
// Sort Colors - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
