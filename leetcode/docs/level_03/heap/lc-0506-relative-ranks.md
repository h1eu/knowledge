---
title: "506. Relative Ranks"
slug: "lc-0506-relative-ranks"
summary: "Heap • Level 3 • Easy — Relative Ranks (LeetCode 506)"
tags: ["leetcode", "heap", "easy", "level-3"]
topic_tags: ["array", "sorting", "heap-priority-queue"]
difficulty: "Easy"
pattern: "Heap"
level: 3
leetcode_id: 506
url: "https://leetcode.com/problems/relative-ranks/"
---

# 506. Relative Ranks

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-easy">Easy</span> <span class="lc-pill lc-pill-pattern">Heap</span> <span class="lc-pill lc-pill-level">Level 3</span> <a href="https://leetcode.com/problems/relative-ranks/" target="_blank">LeetCode 506 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng điểm số <code>score</code> với mỗi phần tử là điểm của vận động viên, hãy trả về mảng hạng: hạng 1 là <code>"Gold Medal"</code>, 2 là <code>"Silver Medal"</code>, 3 là <code>"Bronze Medal"</code>, còn lại là thứ hạng số.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> score = [5,4,3,2,1]
<strong>Đầu ra:</strong> [&quot;Gold Medal&quot;,&quot;Silver Medal&quot;,&quot;Bronze Medal&quot;,&quot;4&quot;,&quot;5&quot;]
<strong>Giải thích:</strong> The placements are [1<sup>st</sup>, 2<sup>nd</sup>, 3<sup>rd</sup>, 4<sup>th</sup>, 5<sup>th</sup>].</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> score = [10,3,8,9,4]
<strong>Đầu ra:</strong> [&quot;Gold Medal&quot;,&quot;5&quot;,&quot;Bronze Medal&quot;,&quot;Silver Medal&quot;,&quot;4&quot;]
<strong>Giải thích:</strong> The placements are [1<sup>st</sup>, 5<sup>th</sup>, 3<sup>rd</sup>, 2<sup>nd</sup>, 4<sup>th</sup>].

</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>n == score.length</code></li>
	<li><code>1 &lt;= n &lt;= 10<sup>4</sup></code></li>
	<li><code>0 &lt;= score[i] &lt;= 10<sup>6</sup></code></li>
	<li>All the values in <code>score</code> are <strong>unique</strong>.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Heap**.

## 💻 Code 5 ngôn ngữ

```cpp
// Relative Ranks - C++
class Solution {}
```

```java
// Relative Ranks - Java
class Solution {}
```

```kotlin
// Relative Ranks - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Relative Ranks - Swift
func solve() -> Int { return 0 }
```

```dart
// Relative Ranks - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
