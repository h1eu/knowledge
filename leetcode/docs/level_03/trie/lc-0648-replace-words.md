---
title: "648. Replace Words"
slug: "lc-0648-replace-words"
summary: "Trie • Level 3 • Medium — Replace Words (LeetCode 648)"
tags: ["leetcode", "trie", "medium", "level-3"]
topic_tags: ["array", "hash-table", "string", "trie"]
difficulty: "Medium"
pattern: "Trie"
level: 3
leetcode_id: 648
url: "https://leetcode.com/problems/replace-words/"
---

# 648. Replace Words

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Trie</span> <span class="lc-pill lc-pill-level">Level 3</span> <a href="https://leetcode.com/problems/replace-words/" target="_blank">LeetCode 648 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho từ điển <code>dictionary</code> (các gốc từ) và câu <code>sentence</code>, hãy thay thế mỗi từ trong câu bằng gốc ngắn nhất trong từ điển mà nó bắt đầu bằng gốc đó. Nếu không có, giữ nguyên.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> dictionary = [&quot;cat&quot;,&quot;bat&quot;,&quot;rat&quot;], sentence = &quot;the cattle was rattled by the battery&quot;
<strong>Đầu ra:</strong> &quot;the cat was rat by the bat&quot;
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> dictionary = [&quot;a&quot;,&quot;b&quot;,&quot;c&quot;], sentence = &quot;aadsfasf absbs bbab cadsfafs&quot;
<strong>Đầu ra:</strong> &quot;a a b c&quot;
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= dictionary.length &lt;= 1000</code></li>
	<li><code>1 &lt;= dictionary[i].length &lt;= 100</code></li>
	<li><code>dictionary[i]</code> consists of only lower-case letters.</li>
	<li><code>1 &lt;= sentence.length &lt;= 10<sup>6</sup></code></li>
	<li><code>sentence</code> consists of only lower-case letters and spaces.</li>
	<li>The number of words in <code>sentence</code> is in the range <code>[1, 1000]</code></li>
	<li>The length of each word in <code>sentence</code> is in the range <code>[1, 1000]</code></li>
	<li>Every two consecutive words in <code>sentence</code> will be separated by exactly one space.</li>
	<li><code>sentence</code> does not have leading or trailing spaces.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Trie**.

## 💻 Code 5 ngôn ngữ

```cpp
// Replace Words - C++
class Solution {}
```

```java
// Replace Words - Java
class Solution {}
```

```kotlin
// Replace Words - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Replace Words - Swift
func solve() -> Int { return 0 }
```

```dart
// Replace Words - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
