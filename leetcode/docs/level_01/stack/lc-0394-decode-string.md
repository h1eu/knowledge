---
title: "394. Decode String"
slug: "lc-0394-decode-string"
summary: "Stack • Level 1 • Medium — Decode String (LeetCode 394)"
tags: ["leetcode", "stack", "medium", "level-1"]
topic_tags: ["string", "stack", "recursion"]
difficulty: "Medium"
pattern: "Stack"
level: 1
leetcode_id: 394
url: "https://leetcode.com/problems/decode-string/"
---

# 394. Decode String

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Stack</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/decode-string/" target="_blank">LeetCode 394 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho chuỗi mã hóa <code>s</code>, hãy giải mã. Quy tắc là <code>k[chuỗi_mã_hóa]</code> nghĩa là chuỗi bên trong ngoặc được lặp <code>k</code> lần. <code>k</code> là số nguyên dương.</p>

<p>Bạn có thể giả định chuỗi luôn hợp lệ, không có khoảng trắng thừa và ngoặc là cân bằng.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;3[a]2[bc]&quot;
<strong>Đầu ra:</strong> &quot;aaabcbc&quot;
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;3[a2[c]]&quot;
<strong>Đầu ra:</strong> &quot;accaccacc&quot;
</pre>

<p><strong class="example">Ví dụ 3:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;2[abc]3[cd]ef&quot;
<strong>Đầu ra:</strong> &quot;abcabccdcdcdef&quot;
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= s.length &lt;= 30</code></li>
	<li><code>s</code> consists of lowercase English letters, digits, and square brackets <code>&#39;[]&#39;</code>.</li>
	<li><code>s</code> is guaranteed to be <strong>a valid</strong> input.</li>
	<li>All the integers in <code>s</code> are in the range <code>[1, 300]</code>.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Stack**.

## 💻 Code 5 ngôn ngữ

```cpp
// Decode String - C++
class Solution {}
```

```java
// Decode String - Java
class Solution {}
```

```kotlin
// Decode String - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Decode String - Swift
func solve() -> Int { return 0 }
```

```dart
// Decode String - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
