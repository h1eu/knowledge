---
title: "904. Fruit Into Baskets"
slug: "lc-0904-fruit-into-baskets"
summary: "Sliding Window • Level 1 • Medium — Fruit Into Baskets (LeetCode 904)"
tags: ["leetcode", "sliding-window", "medium", "level-1"]
topic_tags: ["array", "hash-table", "sliding-window"]
difficulty: "Medium"
pattern: "Sliding Window"
level: 1
leetcode_id: 904
url: "https://leetcode.com/problems/fruit-into-baskets/"
---

# 904. Fruit Into Baskets

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Sliding Window</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/fruit-into-baskets/" target="_blank">LeetCode 904 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Bạn đến thăm vườn cây với một hàng cây, mỗi cây có một loại trái cây, <code>fruits[i]</code> là loại của cây thứ <code>i</code>. Bạn có hai giỏ, mỗi giỏ chỉ chứa một loại. Bắt đầu từ bất kỳ cây nào, bạn di chuyển sang phải và chỉ hái một quả mỗi cây, sao cho mỗi giỏ luôn chỉ chứa một loại. Hãy trả về số quả tối đa có thể hái (tức độ dài mảng con dài nhất chứa tối đa 2 loại khác nhau).</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> fruits = [<u>1,2,1</u>]
<strong>Đầu ra:</strong> 3
<strong>Giải thích:</strong> We can pick from all 3 trees.
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> fruits = [0,<u>1,2,2</u>]
<strong>Đầu ra:</strong> 3
<strong>Giải thích:</strong> We can pick from trees [1,2,2].
If we had started at the first tree, we would only pick from trees [0,1].
</pre>

<p><strong class="example">Ví dụ 3:</strong></p>

<pre>
<strong>Đầu vào:</strong> fruits = [1,<u>2,3,2,2</u>]
<strong>Đầu ra:</strong> 4
<strong>Giải thích:</strong> We can pick from trees [2,3,2,2].
If we had started at the first tree, we would only pick from trees [1,2].
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= fruits.length &lt;= 10<sup>5</sup></code></li>
	<li><code>0 &lt;= fruits[i] &lt; fruits.length</code></li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Sliding Window**.

## 💻 Code 5 ngôn ngữ

```cpp
// Fruit Into Baskets - C++
class Solution {}
```

```java
// Fruit Into Baskets - Java
class Solution {}
```

```kotlin
// Fruit Into Baskets - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Fruit Into Baskets - Swift
func solve() -> Int { return 0 }
```

```dart
// Fruit Into Baskets - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
