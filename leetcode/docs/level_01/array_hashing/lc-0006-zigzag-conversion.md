---
title: "6. Zigzag Conversion"
slug: "lc-0006-zigzag-conversion"
summary: "Array & Hashing • Level 1 • Medium — Zigzag Conversion (LeetCode 6)"
tags: ["leetcode", "array-hashing", "medium", "level-1"]
topic_tags: ["string"]
difficulty: "Medium"
pattern: "Array & Hashing"
level: 1
leetcode_id: 6
url: "https://leetcode.com/problems/zigzag-conversion/"
---

# 6. Zigzag Conversion

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/zigzag-conversion/" target="_blank">LeetCode 6 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho chuỗi <code>s</code> và số hàng <code>numRows</code>, hãy viết chuỗi theo dạng <strong>zigzag</strong> trên <code>numRows</code> hàng rồi đọc theo hàng, trả về chuỗi mới.</p>

<p>Ví dụ với <code>"PAYPALISHIRING"</code> và <code>numRows=3</code> → <code>"PAHNAPLSIIGYIR"</code>.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;PAYPALISHIRING&quot;, numRows = 3
<strong>Đầu ra:</strong> &quot;PAHNAPLSIIGYIR&quot;
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;PAYPALISHIRING&quot;, numRows = 4
<strong>Đầu ra:</strong> &quot;PINALSIGYAHRPI&quot;
<strong>Giải thích:</strong>
P     I    N
A   L S  I G
Y A   H R
P     I
</pre>

<p><strong class="example">Ví dụ 3:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;A&quot;, numRows = 1
<strong>Đầu ra:</strong> &quot;A&quot;
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= s.length &lt;= 1000</code></li>
	<li><code>s</code> consists of English letters (lower-case and upper-case), <code>&#39;,&#39;</code> and <code>&#39;.&#39;</code>.</li>
	<li><code>1 &lt;= numRows &lt;= 1000</code></li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Zigzag Conversion - C++
class Solution {}
```

```java
// Zigzag Conversion - Java
class Solution {}
```

```kotlin
// Zigzag Conversion - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Zigzag Conversion - Swift
func solve() -> Int { return 0 }
```

```dart
// Zigzag Conversion - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
