---
title: "14. Longest Common Prefix"
slug: "lc-0014-longest-common-prefix"
summary: "Array & Hashing • Level 1 • Easy — Longest Common Prefix (LeetCode 14)"
tags: ["leetcode", "array-hashing", "easy", "level-1"]
topic_tags: ["array", "string", "trie"]
difficulty: "Easy"
pattern: "Array & Hashing"
level: 1
leetcode_id: 14
url: "https://leetcode.com/problems/longest-common-prefix/"
---

# 14. Longest Common Prefix

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-easy">Easy</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/longest-common-prefix/" target="_blank">LeetCode 14 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng chuỗi <code>strs</code>, hãy tìm <strong>tiền tố chung dài nhất</strong> trong tất cả chuỗi.</p>

<p>Nếu không có tiền tố chung, trả về chuỗi rỗng <code>""</code>.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> strs = [&quot;flower&quot;,&quot;flow&quot;,&quot;flight&quot;]
<strong>Đầu ra:</strong> &quot;fl&quot;
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> strs = [&quot;dog&quot;,&quot;racecar&quot;,&quot;car&quot;]
<strong>Đầu ra:</strong> &quot;&quot;
<strong>Giải thích:</strong> There is no common prefix among the input strings.
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= strs.length &lt;= 200</code></li>
	<li><code>0 &lt;= strs[i].length &lt;= 200</code></li>
	<li><code>strs[i]</code> consists of only lowercase English letters if it is non-empty.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Longest Common Prefix - C++
class Solution {}
```

```java
// Longest Common Prefix - Java
class Solution {}
```

```kotlin
// Longest Common Prefix - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Longest Common Prefix - Swift
func solve() -> Int { return 0 }
```

```dart
// Longest Common Prefix - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
