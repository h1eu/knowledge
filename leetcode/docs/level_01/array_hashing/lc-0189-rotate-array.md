---
title: "189. Rotate Array"
slug: "lc-0189-rotate-array"
summary: "Array & Hashing • Level 1 • Medium — Rotate Array (LeetCode 189)"
tags: ["leetcode", "array-hashing", "medium", "level-1"]
topic_tags: ["array", "math", "two-pointers"]
difficulty: "Medium"
pattern: "Array & Hashing"
level: 1
leetcode_id: 189
url: "https://leetcode.com/problems/rotate-array/"
---

# 189. Rotate Array

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/rotate-array/" target="_blank">LeetCode 189 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng số nguyên <code>nums</code>, hãy xoay mảng sang phải <code>k</code> bước, thực hiện <strong>in-place</strong>.</p>

<p>Ví dụ xoay <code>[1,2,3,4,5,6,7]</code> với <code>k=3</code> thành <code>[5,6,7,1,2,3,4]</code>.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums = [1,2,3,4,5,6,7], k = 3
<strong>Đầu ra:</strong> [5,6,7,1,2,3,4]
<strong>Giải thích:</strong>
rotate 1 steps to the right: [7,1,2,3,4,5,6]
rotate 2 steps to the right: [6,7,1,2,3,4,5]
rotate 3 steps to the right: [5,6,7,1,2,3,4]
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums = [-1,-100,3,99], k = 2
<strong>Đầu ra:</strong> [3,99,-1,-100]
<strong>Giải thích:</strong> 
rotate 1 steps to the right: [99,-1,-100,3]
rotate 2 steps to the right: [3,99,-1,-100]
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= nums.length &lt;= 10<sup>5</sup></code></li>
	<li><code>-2<sup>31</sup> &lt;= nums[i] &lt;= 2<sup>31</sup> - 1</code></li>
	<li><code>0 &lt;= k &lt;= 10<sup>5</sup></code></li>
</ul>

<p>&nbsp;</p>
<p><strong>Yêu cầu mở rộng:</strong></p>

<ul>
	<li>Try to come up with as many solutions as you can. There are at least <strong>three</strong> different ways to solve this problem.</li>
	<li>Could you do it in-place with <code>O(1)</code> extra space?</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Rotate Array - C++
class Solution {}
```

```java
// Rotate Array - Java
class Solution {}
```

```kotlin
// Rotate Array - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Rotate Array - Swift
func solve() -> Int { return 0 }
```

```dart
// Rotate Array - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
