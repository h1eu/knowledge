---
title: "575. Distribute Candies"
slug: "lc-0575-distribute-candies"
summary: "Array & Hashing • Level 1 • Easy — Distribute Candies (LeetCode 575)"
tags: ["leetcode", "array-hashing", "easy", "level-1"]
topic_tags: ["array", "hash-table"]
difficulty: "Easy"
pattern: "Array & Hashing"
level: 1
leetcode_id: 575
url: "https://leetcode.com/problems/distribute-candies/"
---

# 575. Distribute Candies

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-easy">Easy</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/distribute-candies/" target="_blank">LeetCode 575 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng <code>candyType</code> với <code>candyType[i]</code> là loại kẹo thứ <code>i</code>, bạn có thể ăn <strong><code>n/2</code></strong> viên (với <code>n</code> là tổng số kẹo). Hãy trả về số loại kẹo khác nhau tối đa có thể ăn.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> candyType = [1,1,2,2,3,3]
<strong>Đầu ra:</strong> 3
<strong>Giải thích:</strong> Alice can only eat 6 / 2 = 3 candies. Since there are only 3 types, she can eat one of each type.
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> candyType = [1,1,2,3]
<strong>Đầu ra:</strong> 2
<strong>Giải thích:</strong> Alice can only eat 4 / 2 = 2 candies. Whether she eats types [1,2], [1,3], or [2,3], she still can only eat 2 different types.
</pre>

<p><strong class="example">Ví dụ 3:</strong></p>

<pre>
<strong>Đầu vào:</strong> candyType = [6,6,6,6]
<strong>Đầu ra:</strong> 1
<strong>Giải thích:</strong> Alice can only eat 4 / 2 = 2 candies. Even though she can eat 2 candies, she only has 1 type.
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>n == candyType.length</code></li>
	<li><code>2 &lt;= n &lt;= 10<sup>4</sup></code></li>
	<li><code>n</code>&nbsp;is even.</li>
	<li><code>-10<sup>5</sup> &lt;= candyType[i] &lt;= 10<sup>5</sup></code></li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Distribute Candies - C++
class Solution {}
```

```java
// Distribute Candies - Java
class Solution {}
```

```kotlin
// Distribute Candies - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Distribute Candies - Swift
func solve() -> Int { return 0 }
```

```dart
// Distribute Candies - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
