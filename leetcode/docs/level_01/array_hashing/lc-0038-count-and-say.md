---
title: "38. Count and Say"
slug: "lc-0038-count-and-say"
summary: "Array & Hashing • Level 1 • Medium — Count and Say (LeetCode 38)"
tags: ["leetcode", "array-hashing", "medium", "level-1"]
topic_tags: ["string"]
difficulty: "Medium"
pattern: "Array & Hashing"
level: 1
leetcode_id: 38
url: "https://leetcode.com/problems/count-and-say/"
---

# 38. Count and Say

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/count-and-say/" target="_blank">LeetCode 38 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Dãy <strong>count-and-say</strong> được định nghĩa đệ quy: <code>countAndSay(1) = "1"</code>, với mỗi <code>n &gt; 1</code>, chuỗi là cách đọc chuỗi trước (đếm số lượng ký tự liên tiếp). Ví dụ <code>"1211"</code> đọc là <em>"one 1, one 2, two 1s"</em> → <code>"111221"</code>.</p>

<p>Cho <code>n</code>, hãy trả về chuỗi thứ <code>n</code>.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<div class="example-block">
<p><strong>Đầu vào:</strong> <span class="example-io">n = 4</span></p>

<p><strong>Đầu ra:</strong> <span class="example-io">&quot;1211&quot;</span></p>

<p><strong>Giải thích:</strong></p>

<pre>
countAndSay(1) = &quot;1&quot;
countAndSay(2) = RLE of &quot;1&quot; = &quot;11&quot;
countAndSay(3) = RLE of &quot;11&quot; = &quot;21&quot;
countAndSay(4) = RLE of &quot;21&quot; = &quot;1211&quot;
</pre>
</div>

<p><strong class="example">Example 2:</strong></p>

<div class="example-block">
<p><strong>Input:</strong> <span class="example-io">n = 1</span></p>

<p><strong>Output:</strong> <span class="example-io">&quot;1&quot;</span></p>

<p><strong>Explanation:</strong></p>

<p>This is the base case.</p>
</div>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>1 &lt;= n &lt;= 30</code></li>
</ul>

<p>&nbsp;</p>
<strong>Follow up:</strong> Could you solve it iteratively?
</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Count and Say - C++
class Solution {}
```

```java
// Count and Say - Java
class Solution {}
```

```kotlin
// Count and Say - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Count and Say - Swift
func solve() -> Int { return 0 }
```

```dart
// Count and Say - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
