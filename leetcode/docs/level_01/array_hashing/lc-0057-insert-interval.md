---
title: "57. Insert Interval"
slug: "lc-0057-insert-interval"
summary: "Array & Hashing • Level 1 • Medium — Insert Interval (LeetCode 57)"
tags: ["leetcode", "array-hashing", "medium", "level-1"]
topic_tags: ["array"]
difficulty: "Medium"
pattern: "Array & Hashing"
level: 1
leetcode_id: 57
url: "https://leetcode.com/problems/insert-interval/"
---

# 57. Insert Interval

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/insert-interval/" target="_blank">LeetCode 57 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng các khoảng không chồng lấn và đã sắp xếp <code>intervals</code> và một khoảng mới <code>newInterval</code>, hãy chèn <code>newInterval</code> vào và gộp nếu cần, trả về mảng kết quả.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> intervals = [[1,3],[6,9]], newInterval = [2,5]
<strong>Đầu ra:</strong> [[1,5],[6,9]]
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> intervals = [[1,2],[3,5],[6,7],[8,10],[12,16]], newInterval = [4,8]
<strong>Đầu ra:</strong> [[1,2],[3,10],[12,16]]
<strong>Giải thích:</strong> Because the new interval [4,8] overlaps with [3,5],[6,7],[8,10].
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>0 &lt;= intervals.length &lt;= 10<sup>4</sup></code></li>
	<li><code>intervals[i].length == 2</code></li>
	<li><code>0 &lt;= start<sub>i</sub> &lt;= end<sub>i</sub> &lt;= 10<sup>5</sup></code></li>
	<li><code>intervals</code> is sorted by <code>start<sub>i</sub></code> in <strong>ascending</strong> order.</li>
	<li><code>newInterval.length == 2</code></li>
	<li><code>0 &lt;= start &lt;= end &lt;= 10<sup>5</sup></code></li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Insert Interval - C++
class Solution {}
```

```java
// Insert Interval - Java
class Solution {}
```

```kotlin
// Insert Interval - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Insert Interval - Swift
func solve() -> Int { return 0 }
```

```dart
// Insert Interval - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
