---
title: "28. Find the Index of the First Occurrence in a String"
slug: "lc-0028-find-the-index-of-the-first-oc"
summary: "Array & Hashing • Level 1 • Easy — Find the Index of the First Occurrence in a String (LeetCode 28)"
tags: ["leetcode", "array-hashing", "easy", "level-1"]
topic_tags: ["two-pointers", "string", "string-matching", "z-algorithm", "knuth-morris-pratt-algorithm", "boyer-moore-string-search-algorithm"]
difficulty: "Easy"
pattern: "Array & Hashing"
level: 1
leetcode_id: 28
url: "https://leetcode.com/problems/find-the-index-of-the-first-occurrence-in-a-string/"
---

# 28. Find the Index of the First Occurrence in a String

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-easy">Easy</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/find-the-index-of-the-first-occurrence-in-a-string/" target="_blank">LeetCode 28 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho hai chuỗi <code>haystack</code> và <code>needle</code>, hãy trả về chỉ số lần xuất hiện đầu tiên của <code>needle</code> trong <code>haystack</code>, hoặc <code>-1</code> nếu không tìm thấy.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> haystack = &quot;sadbutsad&quot;, needle = &quot;sad&quot;
<strong>Đầu ra:</strong> 0
<strong>Giải thích:</strong> &quot;sad&quot; occurs at index 0 and 6.
The first occurrence is at index 0, so we return 0.
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> haystack = &quot;leetcode&quot;, needle = &quot;leeto&quot;
<strong>Đầu ra:</strong> -1
<strong>Giải thích:</strong> &quot;leeto&quot; did not occur in &quot;leetcode&quot;, so we return -1.
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= haystack.length, needle.length &lt;= 10<sup>4</sup></code></li>
	<li><code>haystack</code> and <code>needle</code> consist of only lowercase English characters.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Find the Index of the First Occurrence in a String - C++
class Solution {}
```

```java
// Find the Index of the First Occurrence in a String - Java
class Solution {}
```

```kotlin
// Find the Index of the First Occurrence in a String - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Find the Index of the First Occurrence in a String - Swift
func solve() -> Int { return 0 }
```

```dart
// Find the Index of the First Occurrence in a String - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
