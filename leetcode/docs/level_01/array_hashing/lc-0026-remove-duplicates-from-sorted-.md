---
title: "26. Remove Duplicates from Sorted Array"
slug: "lc-0026-remove-duplicates-from-sorted-"
summary: "Array & Hashing • Level 1 • Easy — Remove Duplicates from Sorted Array (LeetCode 26)"
tags: ["leetcode", "array-hashing", "easy", "level-1"]
topic_tags: ["array", "two-pointers"]
difficulty: "Easy"
pattern: "Array & Hashing"
level: 1
leetcode_id: 26
url: "https://leetcode.com/problems/remove-duplicates-from-sorted-array/"
---

# 26. Remove Duplicates from Sorted Array

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-easy">Easy</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/remove-duplicates-from-sorted-array/" target="_blank">LeetCode 26 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng số nguyên <code>nums</code> đã sắp xếp theo thứ tự không giảm, hãy xóa trùng <strong>in-place</strong> sao cho mỗi phần tử khác nhau chỉ xuất hiện một lần, giữ nguyên thứ tự tương đối. Trả về số phần tử khác nhau <code>k</code> (với <code>nums[0..k-1]</code> là kết quả).</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums = [1,1,2]
<strong>Đầu ra:</strong> 2, nums = [1,2,_]
<strong>Giải thích:</strong> Your function should return k = 2, with the first two elements of nums being 1 and 2 respectively.
It does not matter what you leave beyond the returned k (hence they are underscores).
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums = [0,0,1,1,1,2,2,3,3,4]
<strong>Đầu ra:</strong> 5, nums = [0,1,2,3,4,_,_,_,_,_]
<strong>Giải thích:</strong> Your function should return k = 5, with the first five elements of nums being 0, 1, 2, 3, and 4 respectively.
It does not matter what you leave beyond the returned k (hence they are underscores).
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= nums.length &lt;= 3 * 10<sup>4</sup></code></li>
	<li><code>-100 &lt;= nums[i] &lt;= 100</code></li>
	<li><code>nums</code> is sorted in <strong>non-decreasing</strong> order.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Remove Duplicates from Sorted Array - C++
class Solution {}
```

```java
// Remove Duplicates from Sorted Array - Java
class Solution {}
```

```kotlin
// Remove Duplicates from Sorted Array - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Remove Duplicates from Sorted Array - Swift
func solve() -> Int { return 0 }
```

```dart
// Remove Duplicates from Sorted Array - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
