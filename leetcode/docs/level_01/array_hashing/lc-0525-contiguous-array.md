---
title: "525. Contiguous Array"
slug: "lc-0525-contiguous-array"
summary: "Array & Hashing • Level 1 • Medium — Contiguous Array (LeetCode 525)"
tags: ["leetcode", "array-hashing", "medium", "level-1"]
topic_tags: ["array", "hash-table", "prefix-sum"]
difficulty: "Medium"
pattern: "Array & Hashing"
level: 1
leetcode_id: 525
url: "https://leetcode.com/problems/contiguous-array/"
---

# 525. Contiguous Array

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/contiguous-array/" target="_blank">LeetCode 525 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho mảng nhị phân <code>nums</code>, hãy tìm mảng con liên tiếp dài nhất có số lượng <code>0</code> và <code>1</code> bằng nhau.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums = [0,1]
<strong>Đầu ra:</strong> 2
<strong>Giải thích:</strong> [0, 1] is the longest contiguous subarray with an equal number of 0 and 1.
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums = [0,1,0]
<strong>Đầu ra:</strong> 2
<strong>Giải thích:</strong> [0, 1] (or [1, 0]) is a longest contiguous subarray with equal number of 0 and 1.
</pre>

<p><strong class="example">Ví dụ 3:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums = [0,1,1,1,1,1,0,0,0]
<strong>Đầu ra:</strong> 6
<strong>Giải thích:</strong> [1,1,1,0,0,0] is the longest contiguous subarray with equal number of 0 and 1.
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= nums.length &lt;= 10<sup>5</sup></code></li>
	<li><code>nums[i]</code> is either <code>0</code> or <code>1</code>.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Contiguous Array - C++
class Solution {}
```

```java
// Contiguous Array - Java
class Solution {}
```

```kotlin
// Contiguous Array - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Contiguous Array - Swift
func solve() -> Int { return 0 }
```

```dart
// Contiguous Array - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
