---
title: "520. Detect Capital"
slug: "lc-0520-detect-capital"
summary: "Array & Hashing • Level 1 • Easy — Detect Capital (LeetCode 520)"
tags: ["leetcode", "array-hashing", "easy", "level-1"]
topic_tags: ["string"]
difficulty: "Easy"
pattern: "Array & Hashing"
level: 1
leetcode_id: 520
url: "https://leetcode.com/problems/detect-capital/"
---

# 520. Detect Capital

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-easy">Easy</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/detect-capital/" target="_blank">LeetCode 520 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho từ <code>word</code>, hãy kiểm tra việc dùng chữ hoa có đúng không. Đúng khi thuộc một trong ba trường hợp:</p>
<ul>
	<li>Tất cả chữ hoa (<code>"USA"</code>).</li>
	<li>Tất cả chữ thường (<code>"leetcode"</code>).</li>
	<li>Chỉ chữ đầu hoa (<code>"Google"</code>).</li>
</ul>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>
<pre><strong>Đầu vào:</strong> word = "USA"
<strong>Đầu ra:</strong> true
</pre><p><strong class="example">Ví dụ 2:</strong></p>
<pre><strong>Đầu vào:</strong> word = "FlaG"
<strong>Đầu ra:</strong> false
</pre>
<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= word.length &lt;= 100</code></li>
	<li><code>word</code> consists of lowercase and uppercase English letters.</li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Detect Capital - C++
class Solution {}
```

```java
// Detect Capital - Java
class Solution {}
```

```kotlin
// Detect Capital - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Detect Capital - Swift
func solve() -> Int { return 0 }
```

```dart
// Detect Capital - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
