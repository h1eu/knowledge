---
title: "151. Reverse Words in a String"
slug: "lc-0151-reverse-words-in-a-string"
summary: "Array & Hashing • Level 1 • Medium — Reverse Words in a String (LeetCode 151)"
tags: ["leetcode", "array-hashing", "medium", "level-1"]
topic_tags: ["two-pointers", "string"]
difficulty: "Medium"
pattern: "Array & Hashing"
level: 1
leetcode_id: 151
url: "https://leetcode.com/problems/reverse-words-in-a-string/"
---

# 151. Reverse Words in a String

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-medium">Medium</span> <span class="lc-pill lc-pill-pattern">Array & Hashing</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/reverse-words-in-a-string/" target="_blank">LeetCode 151 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho chuỗi <code>s</code>, hãy đảo ngược thứ tự các <strong>từ</strong>. Từ được định nghĩa là dãy ký tự không chứa khoảng trắng. Chuỗi có thể chứa nhiều khoảng trắng thừa đầu/cuối và giữa các từ.</p>

<p>Hãy chuẩn hoá kết quả: chỉ một khoảng trắng giữa các từ, không thừa đầu/cuối.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;the sky is blue&quot;
<strong>Đầu ra:</strong> &quot;blue is sky the&quot;
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;  hello world  &quot;
<strong>Đầu ra:</strong> &quot;world hello&quot;
<strong>Giải thích:</strong> Your reversed string should not contain leading or trailing spaces.
</pre>

<p><strong class="example">Ví dụ 3:</strong></p>

<pre>
<strong>Đầu vào:</strong> s = &quot;a good   example&quot;
<strong>Đầu ra:</strong> &quot;example good a&quot;
<strong>Giải thích:</strong> You need to reduce multiple spaces between two words to a single space in the reversed string.
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>1 &lt;= s.length &lt;= 10<sup>4</sup></code></li>
	<li><code>s</code> contains English letters (upper-case and lower-case), digits, and spaces <code>&#39; &#39;</code>.</li>
	<li>There is <strong>at least one</strong> word in <code>s</code>.</li>
</ul>

<p>&nbsp;</p>
<p><b data-stringify-type="bold">Yêu cầu mở rộng:&nbsp;</b>If the string data type is mutable in your language, can&nbsp;you solve it&nbsp;<b data-stringify-type="bold">in-place</b>&nbsp;with&nbsp;<code data-stringify-type="code">O(1)</code>&nbsp;extra space?</p>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Array & Hashing**.

## 💻 Code 5 ngôn ngữ

```cpp
// Reverse Words in a String - C++
class Solution {}
```

```java
// Reverse Words in a String - Java
class Solution {}
```

```kotlin
// Reverse Words in a String - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Reverse Words in a String - Swift
func solve() -> Int { return 0 }
```

```dart
// Reverse Words in a String - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
