---
title: "4. Median of Two Sorted Arrays"
slug: "lc-0004-median-of-two-sorted-arrays"
summary: "Binary Search • Level 1 • Hard — Median of Two Sorted Arrays (LeetCode 4)"
tags: ["leetcode", "binary-search", "hard", "level-1"]
topic_tags: ["array", "binary-search", "divide-and-conquer"]
difficulty: "Hard"
pattern: "Binary Search"
level: 1
leetcode_id: 4
url: "https://leetcode.com/problems/median-of-two-sorted-arrays/"
---

# 4. Median of Two Sorted Arrays

<div class="lc-problem-header">
  <span class="lc-pill lc-pill-hard">Hard</span> <span class="lc-pill lc-pill-pattern">Binary Search</span> <span class="lc-pill lc-pill-level">Level 1</span> <a href="https://leetcode.com/problems/median-of-two-sorted-arrays/" target="_blank">LeetCode 4 ↗</a>
</div>

## 📋 Đề bài

<div class="lc-description"><p>Cho hai mảng đã sắp xếp <code>nums1</code> và <code>nums2</code> có kích thước <code>m</code> và <code>n</code>, hãy trả về <strong>trung vị</strong> của hai mảng đã sắp xếp.</p>

<p>Độ phức tạp tổng thể phải là <code>O(log (m+n))</code>.</p>

<p>&nbsp;</p><p><strong class="example">Ví dụ 1:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums1 = [1,3], nums2 = [2]
<strong>Đầu ra:</strong> 2.00000
<strong>Giải thích:</strong> merged array = [1,2,3] and median is 2.
</pre>

<p><strong class="example">Ví dụ 2:</strong></p>

<pre>
<strong>Đầu vào:</strong> nums1 = [1,2], nums2 = [3,4]
<strong>Đầu ra:</strong> 2.50000
<strong>Giải thích:</strong> merged array = [1,2,3,4] and median is (2 + 3) / 2 = 2.5.
</pre>

<p>&nbsp;</p>
<p><strong>Ràng buộc:</strong></p>

<ul>
	<li><code>nums1.length == m</code></li>
	<li><code>nums2.length == n</code></li>
	<li><code>0 &lt;= m &lt;= 1000</code></li>
	<li><code>0 &lt;= n &lt;= 1000</code></li>
	<li><code>1 &lt;= m + n &lt;= 2000</code></li>
	<li><code>-10<sup>6</sup> &lt;= nums1[i], nums2[i] &lt;= 10<sup>6</sup></code></li>
</ul>

</div>

## 🎯 Tiếp cận Pattern

Nhận diện khuôn mẫu **Binary Search**.

## 💻 Code 5 ngôn ngữ

```cpp
// Median of Two Sorted Arrays - C++
class Solution {}
```

```java
// Median of Two Sorted Arrays - Java
class Solution {}
```

```kotlin
// Median of Two Sorted Arrays - Kotlin
fun solve(): Int { return 0 }
```

```swift
// Median of Two Sorted Arrays - Swift
func solve() -> Int { return 0 }
```

```dart
// Median of Two Sorted Arrays - Dart
int solve() => 0;
```

## ✅ Checklist

- [ ] C++ - [ ] Java - [ ] Kotlin - [ ] Swift - [ ] Dart
